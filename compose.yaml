services:
  depwatch:
    build: .
    env_file: .env
    user: "1000:1000"
    volumes:
      - ./data:/data
      - ./config.toml:/app/config.toml:ro
    restart: unless-stopped
    logging:
      driver: json-file
      options: { max-size: "10m", max-file: "3" }
